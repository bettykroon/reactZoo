export class Animal {
    constructor(public id: number, public image: string, public isFed: boolean, public lastFed: Date, public latinName: string, public longDesc: string, public meds: string, public name: string, public desc: string, public birthYear: number){}
}